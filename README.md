# Neon Runner

ネオン系サイドスクロールゲーム。HTML5 Canvas + バニラJS。

## 遊ぶ

```bash
index.htmlをブラウザで開く
```

```bash
npx serve .
# → http://localhost:3000
```

SPACE / クリックでジャンプ。


## 自動修正フロー

GitHub の issue に `claude-fix` ラベルを貼ると，
GitHub Actions が自動でコードを修正して PR を提出します。
